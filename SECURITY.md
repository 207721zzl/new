# 安全政策

## 支持范围

安全修复会优先应用到 `main` 分支的最新版本。历史提交和自行修改的部署版本不单独维护。

## 报告漏洞

请使用 GitHub 仓库的 **Security → Report a vulnerability** 私下报告漏洞，不要创建公开 Issue，也不要附上真实账号、密钥或业务文档。

报告中请提供：

- 受影响的版本或提交；
- 可复现的最小步骤；
- 实际影响和可能的攻击条件；
- 已验证的缓解方式（如有）。

适合私下报告的问题包括身份绕过、权限提升、敏感数据泄露、注入、路径穿越、恶意文件解析、服务端请求伪造和依赖供应链风险。

维护者确认问题后会评估影响、准备修复，并在修复可用后协调披露。请在公开讨论前给维护者合理的修复时间。

## 部署方责任

部署方需要保护 `.env`、模型服务、数据库、对象存储和备份，配置 HTTPS 与网络访问控制，并及时更新依赖。Cloudflare Quick Tunnel 仅用于临时演示，不应作为长期生产入口。
